# Currency selector flags

SVGs from [lipis/flag-icons](https://github.com/lipis/flag-icons) (MIT license),
vendored because emoji flags don't render on Chrome for Windows — it falls back
to bare letter pairs like "US". Real images work everywhere.

One 4×3 flag per display currency: us, eu, jp, gb, cn, au, ca, ch, hk, sg.
