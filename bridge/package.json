{
  "name": "tinybiz-printer-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Reads live status from Bambu Lab printers over local MQTT and serves it as JSON for the TinyBiz web app.",
  "bin": {
    "tinybiz-printer-bridge": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "demo": "node index.js --demo"
  },
  "dependencies": {
    "mqtt": "^5.10.1"
  }
}
